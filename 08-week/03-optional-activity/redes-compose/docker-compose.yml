version: '3.9'

services:

  web:
    image: nginx
    ports:
      - "8080:80"
    networks:
      - appnet

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: usuario
      POSTGRES_PASSWORD: password
    networks:
      - appnet

networks:
  appnet: