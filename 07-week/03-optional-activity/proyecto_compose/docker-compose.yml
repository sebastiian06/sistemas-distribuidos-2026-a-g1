version: '3.9'

services:

  web:
    image: nginx
    ports:
      - "8080:80"

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: usuario
      POSTGRES_PASSWORD: password
      POSTGRES_DB: basedatos
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: